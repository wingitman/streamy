# Starter Checklist

- [ ] Keep rendering and hitboxes derived from one layout pass.
- [ ] Reflow and remain bounded after every resize.
- [ ] Keep selected wrapped rows and long content visible.
- [ ] Preserve configurable TOML keybindings and shared themes.
- [ ] Open the theme picker with the configured `theme` key and apply a theme.
- [ ] Rebuild styles and layout after `o` reload or a theme change.
- [ ] Keep the top-right Delbysoft logo clickable and asynchronous.
- [ ] Run tests and all `build-all` targets.
# Generated baseline note

The generated project is expected to satisfy the shared checklist before
domain-specific features are added. Verify the v2 dependencies, complete
configuration/history/update baseline, measured layout, and shared hitboxes.
